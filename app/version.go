package main

// App version. The website returns the latest version in the status webhook response
// (globalSettings.tarkov_pilot_version) — on mismatch the UI shows the Update button.
const Version = "2.0.0"
